[project]
name = "shasta"
version = "0.2.2"
authors = [
  { name="Konstantinos Kallas", email="konstantinos.kallas@hotmail.com" },
]
description = "A shell AST library in Python"
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Topic :: System :: System Shells",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX",
]

[project.optional-dependencies]
dev = ["libbash", "libdash"]
treesitter = [
    "tree-sitter>=0.25; python_version >= '3.10'",
    "tree-sitter-bash>=0.25; python_version >= '3.10'",
]

[project.urls]
"Homepage" = "https://github.com/binpash/shasta"
"Bug Tracker" = "https://github.com/binpash/shasta/issues"
